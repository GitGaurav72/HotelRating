spring.application.name=serviceRegistry


server.port = 8761
# Set the hostname for the Eureka instance
eureka.instance.hostname=localhost

# Disable registering with Eureka (useful for Eureka Server itself)
eureka.client.register-with-eureka=false

# Disable fetching the registry (useful for Eureka Server itself)
eureka.client.fetch-registry=false
